import Component from "$store/components/product/Gallery/ImageSlider.tsx";
import type { Props } from "$store/components/product/Gallery/ImageSlider.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
