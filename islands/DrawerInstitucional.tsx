import Component from "$store/components/institucionals/Controls.tsx";
import type { Props } from "$store/components/institucionals/Controls.tsx";

function Island(props: Props) {
  return <Component {...props} />;
}

export default Island;
