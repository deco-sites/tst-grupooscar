import Component from "site/components/product/Installments.tsx";
import type { Props } from "site/components/product/Installments.tsx";

const Island = (props: Props) => {
  return <Component {...props} />;
};

export default Island;
